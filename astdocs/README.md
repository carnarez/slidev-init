**Pull and format `NumPy`-ish docstrings from Python modules via
[`astdocs`](https://github.com/carnarez/astdocs).**

Users and groups are mounted (read-only) from the host system to avoid ownership issues.
